import { Box, Text } from "grommet";

const ProductManagement = ({ title }: { title: string }) => {
  return (
    <Box>
      <Text>{title}</Text>
    </Box>
  );
};

export default ProductManagement;
